export const submitPersonal = (state) => ({
    type: 'SUBMIT_PERSONAL',
    payload: state,
});

export const submitProfissional = (state) => ({
    type: 'SUBMIT_PROFISSIONAL',
    payload: state,
});